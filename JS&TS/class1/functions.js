// FUNCIONES

// Declarativa
function nombreFunction(parametro1, parametro2) {
    return parametro1 + parametro2;
}

// Expresión
let nombreFunctionExpression = function(parametro1, parametro2) {
    return parametro1 + parametro2;
}